source = $HOME/.config/hypr/accents/rose-pine.conf
source = $HOME/.config/hypr/wallpaper.conf
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# GENERAL
general {
    disable_loading_bar = true
    hide_cursor = true
}

# BACKGROUND
background {
    monitor =
    path = /home/densine/.config/background/in_use
    # path = cmd[] swww query | grep -oP '(?<=: image: ).*' | uniq
    blur_passes = 2
    color = rgba(24273aaa)

}

# TIME
label {
    monitor =
    text = cmd[update:30000] echo "$(date +"%R")"
    color = $text
    font_size = 90
    font_family = $font
    position = -30, 0
    halign = right
    valign = top
}

# DATE
label {
    monitor =
    text = cmd[update:43200000] echo "$(date +"%A, %d %B %Y")"
    color = $text
    font_size = 25
    font_family = $font
    position = -30, -150
    halign = right
    valign = top
}

# USER AVATAR

image {
    monitor =
    path = ~/.face/1_1skala.jpg
    size = 200
    border_color = $subtle

    position = 0, 75
    rounding = -1
    halign = center
    valign = center
}

# INPUT FIELD
#input-field {
#    monitor =
#    size = 300, 60
#    outline_thickness = 4
#    dots_size = 0.2
#    dots_spacing = 0.2
#    dots_center = true
#    outer_color = $accent
#    inner_color = $surface0
#    font_color = $text
#    fade_on_empty = false
#    placeholder_text = <span foreground="##$textAlpha"><i>󰌾   Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
#    hide_input = false
#    check_color = $accent
#    fail_color = $red
#    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#    capslock_color = $yellow
#    position = 0, -35
#    halign = center
#    valign = center
#}
input-field {
     monitor =
    size = 250, 60
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    # outer_color = rgba(0, 0, 0, 0)
    # inner_color = rgba(0, 0, 0, 0.2)
    outer_color = $base
    inner_color = $surface
    # font_color = $textAlpha
    # font_color = rgba(255, 255, 255, 0.6)
    font_color = $text
    fade_on_empty = false
    rounding = -1
    check_color = rgb(204, 136, 34)
    placeholder_text = Input Password...
    hide_input = false
    position = 0, -200
    halign = center
    valign = center
}
